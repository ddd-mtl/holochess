{
  "title": "Move Schema",
  "type": "object",
  "properties": {
    "gameHash": {
      "type": "string",
      "comment": "Hash of challenge it is part of (may be removed in future)"
    },
    "san": {
      "type": "string",
      "comment": "Chess half-move encoded in SAN notation"
    },
    "index": {
      "type": "integer",
      "comment": "Index position of Move in game (may be removed in future)"
    }
  },
  "required": ["gameHash","san", "index"]
}